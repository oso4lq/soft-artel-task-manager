export interface UserData {
    id: string | number;
    username: string;
    img: string;
    email: string;
    telegram: string;
    // tasks?: string[]; // Array with tasks IDs created by this user
}